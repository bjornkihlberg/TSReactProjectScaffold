import { } from './main'

import { expect } from 'chai'

describe('main', () => {
    describe('reducer', () => {
        it('should work', () => {
            const actual = 0
            const expected = 0

            expect(actual).to.equal(expected)
        })
    })
})
